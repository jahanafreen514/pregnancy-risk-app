# Domain or public IP of the Linux VM hosting Coturn
TURN_REALM=turn.example.com
TURN_EXTERNAL_IP=203.0.113.10

# Generate a unique long random value for each; do not reuse application secrets.
TURN_USERNAME=glowcare-turn
TURN_CREDENTIAL=replace-with-a-long-random-turn-password
